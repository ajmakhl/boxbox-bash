import { tv } from 'tailwind-variants';

export const actionButton = tv({
  base: 'rounded-lg text-slate-100 border border-slate-100 cursor-pointer hover:bg-slate-700 focus-visible:bg-slate-700 focus-visible:outline-none',
});
